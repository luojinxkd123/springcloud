spring:
  cloud:
    config:
      uri: http://localhost:8088
      name: apptest
      profile: dev
management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"